# OS / editor cruft
.DS_Store
*.log

# No dependencies are required — the build uses only Node builtins.
node_modules/

# NOTE: docs/ is intentionally committed — GitHub Pages serves the built site from /docs.
